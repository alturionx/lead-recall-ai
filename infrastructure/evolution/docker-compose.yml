services:

  evolution:
    image: evoapicloud/evolution-api:v2.3.7

    container_name: evolution

    restart: unless-stopped

    ports:
      - "8081:8080"

    volumes:
      - ./data:/evolution/instances

    environment:
      - SERVER_URL=http://localhost:8081
      - AUTHENTICATION_API_KEY=LEAD_RECALL_AI_DEV_2026_8c4f5b21a9d74ef7

      - DATABASE_PROVIDER=mysql

      - DATABASE_CONNECTION_URI=mysql://root_docker:alunolab@host.docker.internal:3306/evolution_lead_recall_ai